# 安全与隐私说明

## 核心承诺

- **视频、音频全程只在你的电脑本地处理，不会上传到任何服务器。**
- 本软件**不含账号体系、不收集个人信息、不植入统计/遥测代码、不展示广告**。
- 识别由本地 Whisper 模型完成；模型下载完成后，识别过程可完全断网运行。

## 网络使用情况

| 场景 | 是否联网 | 说明 |
|---|---|---|
| 下载识别模型 | 仅一次 | 安装后运行 `tools/download_model.py` 时从 Hugging Face 下载模型；也可使用镜像或从其他可信设备离线拷贝 |
| 视频/音频识别 | 否 | 纯本地推理 |
| 字幕编辑、SRT/TXT 导出 | 否 | 纯本地运算与本地写文件 |

开源版**不包含**任何 LLM/云端清理功能，不存在向 AI 服务商发送数据的情况。

## 本地文件与数据

- 导入的文件保存在程序目录 `data/uploads/`，提取的音频在 `data/audio/`，导出文件在 `data/outputs/`。
- 原始文件会保留在 `data/` 中（便于你反复校对导出），不会被自动上传或删除。
- 如需彻底清除，直接删除整个 `data/` 目录即可；下次启动会自动重建空目录。

## 网络监听

- 服务**默认只绑定 127.0.0.1（仅本机可访问）**。
- 只有显式设置环境变量 `ALLOW_LAN=1` 时才会监听 0.0.0.0，允许同一局域网设备访问；
  此时请确认你所处网络可信，用完后重启服务（不带该变量）即可恢复仅本机模式。

## 建议

- 在公用/共享电脑上使用后，请删除 `data/` 目录并清理浏览器下载记录。
- 仅从本项目官方仓库获取源码，不要运行来源不明的打包版本。
